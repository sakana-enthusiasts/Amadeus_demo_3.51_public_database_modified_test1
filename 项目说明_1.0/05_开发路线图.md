# 开发路线图

按功能域组织，依据当前真实代码与验证。`[x]` 仅用于功能已实现、已接实际流程且相关验证通过；接口、占位、未接入或未来计划一律 `[ ]`。后续开发完成后据代码和测试更新，不按旧版本历史补勾。未决定的实现只记待定义，不构成开发授权。

## 公共数据与证据

- [x] 独立公开聚合入口、ProviderRegistry 与 PubChem/CompTox/NIST/ThermoML 策略、raw cache、Repository 多后端、条件/单位/形式匹配与冲突保留（控制器接入；公开证据离线/模拟测试验证）。
- [ ] 公开物性接入其他业务流程（当前独立查询已可用，下游接入范围待定义；不覆盖旧宽表）。

## 化合物身份与结构

- [x] 用户流程中的身份映射、冲突状态、RDKit 结构与描述符；公开 observed/parent 形式区分和身份冲突隔离（用户流程及公开证据测试验证）。
- [ ] 身份入口与稳定全局 ID 边界统一（现有兼容回退和页面越界仍有技术债，后续范围待定义）。

## 候选筛选

- [x] 用户 CSV/XLSX 导入、字段映射、run_id 隔离、HSP/属性/规则评价与报告（临时目录端到端与规则测试验证）。
- [ ] 完整解除用途前置计算绑定（通用规则已接入，其他部分仍受当前场景限制；待定义）。

## Target Profile

- [ ] Target Profile 功能（独立数据契约、编辑与 JSON 存取已实现并通过测试，但未接 UI、生成、筛选、物性调度或 Pareto，功能未完成）。

## 配方生成

- [x] 完整配方输入组装、明确单位换算、独立候选池粗搜、约束评价和 Pareto、最后 Benchmark 对照隔离（研发流程与相关测试验证）。
- [ ] 局部比例细化正确进入再评价（插件和循环已接入，但默认试跑仍有基准错误；见 TD-01）。

## 配方物性

- [x] 五类统一物性、实测优先、模型注册/选择、auto 回退、声明依赖和替换；含 Cheng_2008 甘油–水黏度（用途/研发接入，物性及研发测试验证）。
- [ ] 模型实验误差与不确定度校准（现有数值/参考检查不等于全部指标独立实验验证，具体方案待定义）。

## 毒理

- [x] 本地/人工证据三态会话、条件匹配/数据缺口与 PubChem GHS 提示隔离（控制器接入，毒理匹配测试验证）。
- [ ] 旧信息视图/报告标识和研究式预留整理（真实问题已记录，处理方式待用户决定）。

## 预测模型

- [ ] 透明化预测（统一预测接口已预留，GP 插件未启用，无真实训练产物，功能未完成）。

## 实验数据闭环

- [x] 候选草稿/冻结批次、用途终点读取与特征状态输出（确认和用途流程已接入，批次/用途测试验证）。
- [ ] 训练、验证与实验结果的完整闭环（当前已有记录入口，闭环未完成，具体实现待定义）。

## 实验优化

- [ ] 下一轮实验优化（统一优化接口已预留，BoTorch 未启用，功能未完成）。

## UI

- [x] 配方构建物性录入与独立研发页面调用已有核心流程（相关 AppTest 验证）。
- [ ] 后续 UI 调整（待定义；当前任务明确不改 UI）。

## 上层自动化 / Agent

- [ ] 上层自动化 / Agent（未来可能作为稳定接口和现有流程的调用者，具体实现待定义；当前不实现、不建空框架，底层不依赖它）。
