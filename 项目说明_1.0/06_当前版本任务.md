# 当前版本任务

当前版本：3.51。用户以整个版本目录复制方式迭代；复制到 3.52 后，根据新任务修改或替换本文件，不通过新代码外壳管理版本。

## 当前阶段

当前正在整理公开数据库能力与公开证据体系，使公开数据层作为独立、可替换模块稳定下来，并建立可靠的 AI 开发事实来源。真实代码已经形成 PubChem、CompTox、NIST、ThermoML Provider，统一 PublicDataProvider/Registry、Repository/Resolver，以及条件、单位、身份与冲突处理和 raw cache。这些能力是现有实现，本轮不重新开发。

## 本轮授权与完成目标

只检查现有代码、重写/迁移说明、清理用户明确指定的过时说明与无关研究/历史/缓存产物。建立本目录固定的 `00`–`07` 和三个模块说明，重写根 AGENTS 与当前 README，删除平行内部说明正文，保留运行所需数据、测试、许可及依赖声明。

必须保留公开证据“公共契约 + 可替换来源/存储 + 上层编排”和配方物性“独立指标 + 注册选择 + 声明依赖”的现有正确模式，准确区分接口、运行能力和未接入项。对预留与架构债务只分类记录。

## 当前不属于任务

不改 UI，不新增功能，不重构业务算法，不实现 Agent、不创建空 Agent 框架，不为未来自动调度改代码。不因其他模块落后顺手重构整个项目；其他模块以后分别按明确任务处理。不得自行删除或实现业务中的口服、药代、delivery、未启用插件及其他预留。

不 commit、不 push、不创建 PR、不发布版本。配置值与运行所需数据保持不变；清理限于用户授权的说明、历史运行输出、无实际模型的空目录和缓存/临时研究产物。

## 完成验证

核对目录和真实代码后再迁移，核对 Markdown 链接、旧上下文残留、路线图 checkbox、版本任务范围、Cheng_2008 记录与中性 Target Profile 示例。相关测试使用内存或临时数据根目录；不得为验证覆盖真实项目结果。最终比较业务源码、设置、依赖、许可与数据内容，确认完全未修改，并向用户输出删除/迁移/重写与保留内容清单。
