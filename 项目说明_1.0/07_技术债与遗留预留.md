# 技术债与遗留预留

核对日期：2026-09-17。重新扫描当前代码，按“当前真实功能 / 合理通用扩展点 / 历史预留 / 疑似研究计划污染”分类；分类是维护判断，不声称已经追溯每项历史来源。本轮均不修改、删除或实现业务代码。

## 预留与研究倾向专项核对

| 当前出现的位置/内容 | 分类与实际状态 | 后续边界 |
| --- | --- | --- |
| `毒性证据视图插件.py` 输出 `未来人体口服模式结果`，毒理页面读取该标识 | 历史预留，疑似研究计划污染；实际执行的是逐候选信息/缺口视图，字段说明不作人体安全结论，不是人体口服模型 | 不把口服方向写成平台默认需求；由用户以后决定保留/改名/移除 |
| `筛选报告导出插件.py` 读取 `未来人体口服模型结果`、`当前动物实验模型结果` | 历史兼容/契约债务；视图实际保存的是 `未来人体口服模式结果`、`当前动物实验模式结果`，读取缺失会返回空表 | 报告可能显示无记录；不能将历史标识当有效模型功能，当前不修复 |
| 人体视图字段“人体代谢或药代信息”“代谢接口状态”“BBB接口状态” | 历史预留，疑似研究计划污染；内容为接口未实现/仅接口或不足状态，没有正式本地药代/BBB 算法或 Repository 契约 | 不扩展为人体代谢/药代开发任务 |
| 人体视图“动物口服重复给药数据”与终点字典“重复给药” | 口服专用视图属于历史预留/疑似研究倾向；重复暴露和终点分类本身是合理通用毒理描述，当前没有口服重复给药预测模块 | 区分通用证据字段和特定研究方向，不自动删除或实现 |
| `核心系统/目标规格/数据结构.py` 允许 `delivery` scope | 合理通用扩展点；作用域枚举和校验已实现，执行层未接入 | 不代表已有递送模块，不因枚举创建新目录/功能 |
| `systemic_pk`、`metabolism` 出现在目标规格测试及冻结外部评估配置/runner | 测试中的历史/研究式指标预留；生产 Target Profile 允许扩展 ID，但没有内置对应模型，冻结 runner 也不是生产执行层 | 测试/数据保留；通用说明示例使用中性规格，不把药代指标当默认需求 |
| `CompToxProvider.fetch_toxicokinetics` 及 CTX toxicokinetics 来源领域 | 当前真实功能；公开 Provider 获取远程证据，保留来源类型/模型信息，并非本地人体药代模型或口服安全模块 | 不把证据抓取等同于预测、旧毒理匹配接入或安全结论 |
| `核心系统/毒理分析.py` 的来源状态中 CompTox/ToxRefDB、CE 与后续来源未查询 | 旧会话通道历史预留；新的 CompTox Provider 已实现，旧毒理公开入口仍仅支持 GHS | 两个通道状态应分别解释，不称全平台 CompTox 未实现 |
| GP、BoTorch、候选评分、独立官能团插件继承 `未启用插件` | 未启用具体功能的历史预留；只返回状态字典 | 不打完成勾，不自动训练或启用；RDKit 自己已有 SMARTS 分类不等于独立插件已启用 |
| 统一预测/优化接口的 abstractmethod | 合理通用扩展点，具体模型功能未完成 | 抽象方法不是错误或必须立即实现的 TODO |
| `设置/字段对应设置.py`、预测模型/实验优化/评分权重设置文件 | 纯注释历史占位，无对应设置实现 | 用户导入已有显式字段映射，不能因空设置文件说现有导入无映射 |
| 确认草稿中的“透明化计算接口状态=已预留，待接入” | 历史状态字段；确认后的描述符与用途评估已经自动接入，但训练模型仍未启用 | 不能只按该字段否定已接入流程，也不能声称模型已实现 |
| 日志管理器 | 合理公共入口但持久化策略未完成，仅 logging.getLogger | 已有 raw/运行元数据并不等于统一全平台运行历史 |

未从生产代码发现已实现的 Agent 或独立递送框架；不创建空框架。研究式测试配置保留原样，不作为产品需求。

## 当前真实技术债

### TD-01 比例细化混用绝对用量和分数

配方输入组装把深拷贝定义中的分数改写为 g/mol/mL 绝对用量并返回；比例优化从预测配方的定义读取用量，自动组合又将其标为质量分数。100 g 基准的 20/80 会变成 19.95/80.05 等并作为分数送回。默认演示纯内存运行粗搜 52、累计定义 60、排序 52、细化组装失败 8。现有隔离测试明确保留旧失败行为，不证明完整比例优化已完成。

### TD-02 功能插件私有调用和内部函数依赖

确认批次描述符插件实例化 RDKit普通描述符插件，调用 `_描述符值/_Fig1c标签`；比例优化导入自动组合的 `组装定义`，发现约束导入比例优化的 `组成签名`。这些依赖妨碍替换，尤其私有调用违反稳定契约原则。共享公开纯算法契约和业务插件互调需区分，当前不扩大耦合。

### TD-03 界面越界与用途绑定

配方构建页面导入核心私有 `_配置对象`，身份审核页面直接调用人工确认功能插件。确认草稿与用途评估明确限制当前颅骨用途，用户 HSP 前置计算仍绑定 SeeThrough 参照。公共平台定位不等于这些代码已通用化；当前不改 UI 或业务流程。

### TD-04 接口完整性和替换注入不足

功能插件上下文/返回 Any，注册表不验证且同标识覆盖；数据管理抽象接口未声明下游需要的软件数据库写入等方法。研发生成/约束/排序直接实例化，只有物性等能注入。公开聚合允许 ProviderRegistry、Repository、raw cache 注入，但直接构造 EvidenceResolver，尚无 Resolver 注入点。不得描述为全平台所有实现均可无改动替换。

### TD-05 数据与标识边界

旧文件数据管理只保护论文原始目录，其他表格/导出标识未全面限制在目标目录；运行管理对上传/导出/运行路径增强检查，但继承表格拼接仍有缺口。公开身份接受本地候选编号回退为全局 compound_id，共享证据库存在跨运行碰撞风险；已有 observed structure 隔离不能涵盖空结构场景。

### TD-06 近似与缺失覆盖不足

用途描述符聚合根据单位包含 `%`/“百分”判定可加权，并对有值成分重新归一化；可能混基准、在部分缺失时仍输出近似值。虽然已标记筛选近似，不能当完整真实混合物物性。直接物性入口部分温度/波长一致性依赖来源，校验弱于完整组装；公式区间与误差大多未量化。

### TD-07 多种存储路径与旧报告契约

用户 run_id 目录、旧固定结果目录、全局确认/用途 CSV 与 CLI 根报告目录并存；用途同批次覆盖而非不可变历史。人体/动物模式与模型结果标识不一致已在专项表说明。删除历史根运行输出不改变这些代码约定；`数据/` 内当前所需数据不清理。

### TD-08 兼容层与静态未使用线索

工厂旧别名、PubChem 旧类/插件标识、CompTox 旧类名、用途运行器反向转发和最终结果回退仍存在，兼容意义明确，不能机械删除。静态看到数据管理的 Any、毒理分析的 field、PubChem parser 的 replace 导入未被本文件使用；这只是未使用导入线索，不足以宣称整块逻辑是死代码。异常类空 body、SQLite 自动事务钩子和 Parquet close 的 pass 都有当前实现理由，不视为未实现功能。

### TD-09 部分测试写真实项目数据

部分论文端到端/插件单测/图表测试以真实根目录执行并覆盖结果，本轮不运行这些测试。后续回归需临时数据根或用户明确产物范围。冻结 benchmark 与创建发布请求的辅助脚本是本地测试材料，保留但不自动执行。

## 处理规则

上述未完成、研究倾向和技术债都不是当前功能开发授权。公共 Provider/Repository/Resolver、五类物性、完整组装、粗搜/Pareto、确认用途流程等已存在，不能再沿用旧描述称它们全未实现。未来改动必须重新核对并更新本文件、当前架构和路线图。
